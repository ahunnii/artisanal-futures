import "@testing-library/jest-dom";
import { render, screen } from "@testing-library/react";

describe("Drivers", () => {
  //   it('creates and adds new drivers with vehicle defaults to depot', () => { })
  //   it('creates and adds new drivers with vehicle defaults to depot and route', () => { })
  //   it('creates and adds new drivers with vehicle defaults to depot', () => { })
});
